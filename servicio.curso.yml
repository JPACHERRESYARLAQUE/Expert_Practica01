#HTTP Server
server:
  port: 1111

#Discovery Server Access
eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka
  instance:
    status-page-url-path: /infoCurso
    health-check-url-path: /healthCurso
    metadata-map:
       app-owner: pacherres
       description: test application for the expert course
